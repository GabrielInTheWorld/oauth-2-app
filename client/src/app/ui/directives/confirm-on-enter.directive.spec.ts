import { ConfirmOnEnterDirective } from './confirm-on-enter.directive';

describe('ConfirmOnEnterDirective', () => {
    it('should create an instance', () => {
        const directive = new ConfirmOnEnterDirective(null);
        expect(directive).toBeTruthy();
    });
});
